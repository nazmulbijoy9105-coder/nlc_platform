# =============================================================================
# GitHub Actions: Auto-Deploy Frontend to Vercel on Push
# =============================================================================
#
# Required GitHub Secrets:
#   NLC_FRONTEND_PAT  - Fine-grained PAT with read access to nlc_frontend repo
#   VERCEL_TOKEN      - Vercel deploy token
#   VERCEL_ORG_ID     - Vercel org/team ID
#   VERCEL_PROJECT_ID - Vercel project ID for nlc-frontend
#
# =============================================================================

name: Deploy Frontend to Vercel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    # Skip if the required Vercel secrets aren't configured yet
    if: ${{ vars.VERCEL_DEPLOY_ENABLED != 'false' }}

    steps:
      - name: Check required secrets
        run: |
          if [ -z "${{ secrets.NLC_FRONTEND_PAT }}" ]; then
            echo "::warning::NLC_FRONTEND_PAT secret is not set. Add it in Settings > Secrets > Actions."
            echo "Skipping frontend deployment."
            exit 0
          fi
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "::warning::VERCEL_TOKEN secret is not set. Add it in Settings > Secrets > Actions."
            exit 1
          fi

      - uses: actions/checkout@v4
        with:
          repository: nazmulbijoy9105-coder/nlc_frontend
          token: ${{ secrets.NLC_FRONTEND_PAT }}

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Deploy to Vercel Production
        id: vercel_deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          DEPLOY_URL=$(vercel deploy \
            --prod \
            --token=$VERCEL_TOKEN \
            --yes 2>&1 | tail -1)
          echo "deploy_url=$DEPLOY_URL" >> $GITHUB_OUTPUT
          echo "Deployed to: $DEPLOY_URL"

      - name: Health Check
        run: |
          sleep 20
          TARGET_URL="${{ steps.vercel_deploy.outputs.deploy_url }}"
          if [ -z "$TARGET_URL" ]; then
            TARGET_URL="https://nlc-frontend.vercel.app"
          fi
          echo "Checking: $TARGET_URL"
          curl -f -s --max-time 30 "$TARGET_URL" > /dev/null && \
            echo "Health check passed" || \
            (echo "::warning::Health check failed for $TARGET_URL" && exit 0)
