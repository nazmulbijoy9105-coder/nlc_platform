# Development/CI tools — not needed at runtime
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
ruff==0.15.12
mypy==1.20.2
pytest==8.3.5
pytest-asyncio==0.25.2
pytest-cov==6.0.0
